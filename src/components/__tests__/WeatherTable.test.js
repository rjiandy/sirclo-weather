import React from 'react';
import WeatherTable from '../WeatherTable';
import renderer from 'react-test-renderer';

it('renders weather table without anything correctly', () => {
  // const tree = renderer
  //   .create(<WeatherTable />)
  //   .toJSON();
  // expect(tree).toMatchSnapshot();
  // NOTE: Jest not working on rendering JSX (need further troubleshooting)
});